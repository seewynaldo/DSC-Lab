#include <stdio.h>

int main() {
    int num1 = 12;  
    int num2 = 8;   
    int result;     

    result = num1 ^ num2;  

    printf("The result of %d XOR %d is %d\n", num1, num2, result);

    return 0;
}
